# Dependencies
node_modules/

# Build output
dist/

# Logs
*.log
launcher.log

# Personal data — kabhi push nahi hoga
data/
*.zip

# Android binaries — generated by CI, never commit them
*.apk

# OS
.DS_Store
Thumbs.db

# but src/data is source data — commit it
!src/data/

# signing key — decoded per-build in CI, never committed
jee-planner.keystore
